const SectionTitle = ({ name }: { name: string }) => {
  return (
    <h3 className="text-center text-4xl py-24 font-extrabold capitalize">
      {name}
    </h3>
  );
};

export default SectionTitle;
